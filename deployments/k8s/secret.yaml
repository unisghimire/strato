# TEMPLATE ONLY — never commit real values. In production, populate via
# ExternalSecrets/SealedSecrets/SOPS from your secret manager.
apiVersion: v1
kind: Secret
metadata:
  name: strato-secrets
  namespace: strato
type: Opaque
stringData:
  STRATO_POSTGRES_PASSWORD: "CHANGE-ME"
  STRATO_S3_ACCESS_KEY: "CHANGE-ME"
  STRATO_S3_SECRET_KEY: "CHANGE-ME"
  STRATO_AUTH_JWT_SECRET: "CHANGE-ME-openssl-rand-base64-48"
  STRATO_ENCRYPTION_MASTER_KEY: "CHANGE-ME-openssl-rand-base64-32"
