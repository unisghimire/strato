run:
  timeout: 5m
  build-tags:
    - integration

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gosec
    - gocritic
    - revive
    - misspell
    - unconvert
    - unparam
    - bodyclose
    - noctx
    - sqlclosecheck
    - rowserrcheck
    # contextcheck is intentionally omitted: Strato detaches contexts on
    # purpose in cleanup/shutdown paths (context.WithoutCancel + timeout),
    # which contextcheck cannot distinguish from bugs.
    - errorlint
    - goimports

linters-settings:
  goimports:
    local-prefixes: github.com/unisghimire/strato
  gosec:
    excludes:
      - G404 # math/rand is acceptable for jitter, never used for secrets
  revive:
    rules:
      - name: exported
        disabled: false

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - gosec
        - noctx
